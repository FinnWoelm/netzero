<%= form_for(@reduction_option) do |f| %>
  <% if @reduction_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reduction_option.errors.count, "error") %> prohibited this reduction_option from being saved:</h2>

      <ul>
      <% @reduction_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label :carbon_impact %><br>
    <%= f.text_field :carbon_impact %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
